import os

DATABASE_URL = os.environ["DATABASE_URL"]


def get_users(active: bool):
    return sql(t"""
SELECT * FROM users
WHERE active = {active}
""")


def get_count():
    total = 42
    return sql(t"SELECT count(*) FROM orders")
